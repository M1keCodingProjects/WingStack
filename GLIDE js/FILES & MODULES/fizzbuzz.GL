use "std GLib" with std

def fizzbuzz with n { 
  make options = n "Fizz" "Buzz" "FizzBuzz"
  exit options[ n dup 3 % 0 dup rot> == rot> 5 % == 2 * + ]
}

loop 100 with n {
  when n not { next }
  print fizzbuzz( n )
}