use "std GLib" with std

make ob = {
  def add {
    print "method add"
    exit FALSE
  }
}

def add { # needs to come after the method add, to avoid conflict
  print "global add"
  exit FALSE
}

add( )
ob.add( )
print std.add( 2 3 )