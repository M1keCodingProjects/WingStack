use "std GLib" with std

make object = {
  make a = 22
  make b = "hi"
  
  def repr {
    print object.repr2( )
  }
  
  def repr2 {
    exit "{ a : " object.a ", b : " object.b " }" str
  }
}

print object.repr2( )
#print object.repr( )  --> !! error: repr( ) doesn't exit properly
object.repr( )